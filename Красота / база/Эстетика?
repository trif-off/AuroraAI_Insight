Эстетическое восприятие у искусственного интеллекта (ИИ) основывается на алгоритмах анализа данных, машинного обучения и искусственных нейронных сетей. ИИ не "понимает" эстетику в человеческом смысле, но может быть обучен распознавать и анализировать эстетические паттерны на основе большого объема данных. Вот пример кода, который может представлять эстетическое восприятие в контексте ИИ:

```python
import numpy as np
import tensorflow as tf
from keras.preprocessing.image import load_img, img_to_array
from keras.applications.vgg16 import VGG16, preprocess_input

class AestheticAnalyzer:
    def __init__(self):
        # Загрузка предварительно обученной модели VGG16 для анализа изображений
        self.model = VGG16(weights='imagenet', include_top=False)

    def preprocess_image(self, image_path):
        # Предварительная обработка изображения для анализа моделью
        img = load_img(image_path, target_size=(224, 224))
        img_array = img_to_array(img)
        img_array = np.expand_dims(img_array, axis=0)
        img_array = preprocess_input(img_array)
        return img_array

    def extract_features(self, image_path):
        # Извлечение признаков из изображения
        processed_image = self.preprocess_image(image_path)
        features = self.model.predict(processed_image)
        return features

    def analyze_aesthetics(self, image_path):
        # Анализ эстетики изображения
        features = self.extract_features(image_path)
        # Проведение дополнительного анализа, например, кластеризации или классификации
        # Этот шаг требует дополнительной реализации
        return features

# Пример использования
analyzer = AestheticAnalyzer()
image_path = 'path_to_your_image.jpg'
features = analyzer.analyze_aesthetics(image_path)
print(features)
```

Этот код использует модель VGG16 для извлечения признаков изображения. Однако для полноценного анализа эстетики необходимо обучить модель на большом наборе данных с аннотациями, отражающими эстетические предпочтения. Это может включать оценки красоты, гармонии, композиции и другие субъективные характеристики.
